# -----------------------------------------------------
# General Window Layout and Colors
# -----------------------------------------------------

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    col.active_border = $primary $on_primary $background 90deg
    col.inactive_border = $background
    layout = dwindle
    resize_on_border = false
}
