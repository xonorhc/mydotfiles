# -----------------------------------------------------
# Key Bindings
# -----------------------------------------------------

# SUPER KEY
$mainMod = SUPER

# MY PROGRAMS
$scripts = ~/.config/hypr/scripts

# Applications
$terminal = uwsm app -- kitty
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod, E, exec, $terminal -e yazi
bind = $mainMod, C, exec, $terminal -e nvim
bind = $mainMod SHIFT, N, exec, $terminal -e jrnl study
bind = $mainMod SHIFT, D, exec, $terminal -e nb shell

$terminalFloat = [float; size (monitor_w/2) (monitor_h/1.5)] $terminal
bind = $mainMod SHIFT, RETURN, exec, $terminalFloat
bind = CTRL SHIFT, ESCAPE, exec, $terminalFloat -e btop
bind = $mainMod, D, exec, $terminalFloat -e $scripts/nb_daily
bind = $mainMod, N, exec, $terminalFloat -e jrnl --config-override editor 'nvim -c "startinsert"'

$browser = uwsm app -- flatpak run app.zen_browser.zen
bind = $mainMod, W, exec, uwsm-app $($browser)
bind = $mainMod SHIFT, W, exec, uwsm-app $($browser --private-window)

bind = $mainMod SHIFT, E, exec, uwsm app -- thunar

# Display
bind = $mainMod SHIFT, mouse_down, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') + 0.5}")
bind = $mainMod SHIFT, mouse_up, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') - 0.5}")
bind = $mainMod SHIFT, Z, exec, hyprctl keyword cursor:zoom_factor 1

# Windows
bind = $mainMod, Q, killactive                                                             
bind = $mainMod, F, fullscreen, 0                                                          
bind = $mainMod, M, fullscreen, 1                                                          
bind = $mainMod, T, togglefloating                                                         
bind = $mainMod, G, togglegroup                                                            
bind = $mainMod SHIFT, J, layoutmsg, togglesplit                                                 
bind = $mainMod SHIFT, K, layoutmsg, swapsplit                                                   
bindm = $mainMod, mouse:272, movewindow                                                    
bindm = $mainMod, mouse:273, resizewindow                                                  
bind = $mainMod SHIFT, right, resizeactive, 100 0                                          
bind = $mainMod SHIFT, left, resizeactive, -100 0                                          
bind = $mainMod SHIFT, down, resizeactive, 0 100                                           
bind = $mainMod SHIFT, up, resizeactive, 0 -100                                            
bind = $mainMod ALT, left, swapwindow, l                                                   
bind = $mainMod ALT, right, swapwindow, r                                                  
bind = $mainMod ALT, up, swapwindow, u                                                     
bind = $mainMod ALT, down, swapwindow, d                                                   

# Window - Move focus
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d
bind = $mainMod, left, movefocus, l                                                        
bind = $mainMod, right, movefocus, r                                                       
bind = $mainMod, up, movefocus, u                                                          
bind = $mainMod, down, movefocus, d                                                        
binde = ALT,Tab,cyclenext                                                                  
binde = ALT,Tab,bringactivetotop                                                           

# Actions
bind = $mainMod SHIFT, R, exec, hyprctl reload
bind = $mainMod SHIFT, B, exec, ~/.config/waybar/launch
bind = ALT_R, Control_R, exec, killall -SIGUSR1 waybar
bind = $mainMod CTRL, Q, exec, $scripts/power-menu
bind = $mainMod CTRL, L, exec, $scripts/power lock                      
bind = $mainMod CTRL, P, exec, hyprpicker -a
bind = CTRL, RETURN, exec, playerctl play-pause

$rofiConfig = '~/.config/rofi/'
bind = $mainMod, A, exec, rofi -show drun
bind = SHIFT, Tab, exec, rofi -show window -replace -config $rofiConfig/config-compact.rasi
bind = $mainMod, V, exec, cliphist list | rofi -dmenu -display-columns 2 -replace -config $rofiConfig/config-cliphist.rasi | cliphist decode | wl-copy
bind = $mainMod, S, exec, $scripts/rofi-websearch
bind = $mainMod SHIFT, A, exec, $scripts/rofi-translator
bind = $mainMod SHIFT, C, exec, $scripts/rofi-colorselector

$screenshot = $scripts/hyprshot -o 'Pictures/screenshots' -m active
bind = $mainMod, P, exec, $screenshot -m window
bind = $mainMod ALT, P, exec, $screenshot -m output                       
bind = $mainMod SHIFT, P, exec, $screenshot -m region

# Workspaces
bind = $mainMod, 1, workspace, 1 
bind = $mainMod, 2, workspace, 2 
bind = $mainMod, 3, workspace, 3 
bind = $mainMod, 4, workspace, 4 
bind = $mainMod, 5, workspace, 5 
bind = $mainMod, 6, workspace, 6 
bind = $mainMod, 7, workspace, 7 
bind = $mainMod, 8, workspace, 8 
bind = $mainMod, 9, workspace, 9 
bind = $mainMod, 0, workspace, 10

bind = $mainMod SHIFT, 1, movetoworkspace, 1 
bind = $mainMod SHIFT, 2, movetoworkspace, 2 
bind = $mainMod SHIFT, 3, movetoworkspace, 3 
bind = $mainMod SHIFT, 4, movetoworkspace, 4 
bind = $mainMod SHIFT, 5, movetoworkspace, 5 
bind = $mainMod SHIFT, 6, movetoworkspace, 6 
bind = $mainMod SHIFT, 7, movetoworkspace, 7 
bind = $mainMod SHIFT, 8, movetoworkspace, 8 
bind = $mainMod SHIFT, 9, movetoworkspace, 9 
bind = $mainMod SHIFT, 0, movetoworkspace, 10

bind = $mainMod, Tab, workspace, m+1      
bind = $mainMod SHIFT, Tab, workspace, m-1

bind = $mainMod, mouse_down, workspace, e+1 
bind = $mainMod, mouse_up, workspace, e-1   
bind = $mainMod CTRL, down, workspace, empty

# Fn keys
bind = , XF86MonBrightnessUp, exec, brightnessctl -q s +10%                                                                                                 
bind = , XF86MonBrightnessDown, exec, brightnessctl -q s 10%-                                                                                               
bindle=, XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+                                                                         
bindle=, XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-                                                                              
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle                                                                                     
bind = , XF86AudioPlay, exec, playerctl play-pause                                                                                                          
bind = , XF86AudioPause, exec, playerctl pause                                                                                                              
bind = , XF86AudioNext, exec, playerctl next                                                                                                                
bind = , XF86AudioPrev, exec, playerctl previous                                                                                                            
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle                                                                              
bind = , XF86Lock, exec, hyprlock                                                                                                                           

bind = , code:238, exec, brightnessctl -d smc::kbd_backlight s +10
bind = , code:237, exec, brightnessctl -d smc::kbd_backlight s 10-
