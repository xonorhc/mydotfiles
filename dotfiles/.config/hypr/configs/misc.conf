# -----------------------------------------------------
# Misc Settings
# -----------------------------------------------------

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    initial_workspace_tracking = 1
    on_focus_under_fullscreen = 1
    allow_session_lock_restore = true
    background_color = $inverse_primary
}

# XWayland
xwayland {
  force_zero_scaling = true
}
